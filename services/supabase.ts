export const supabaseConfig = { url: process.env.EXPO_PUBLIC_SUPABASE_URL, anonKey: process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY, enabled: false };
// Future: initialize Supabase client here when online data, auth and RLS policies are ready.
